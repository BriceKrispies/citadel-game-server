<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- The harness is a CLIENT: it speaks the real binary protobuf protocol via the
         canonical codec/contract. It does not depend on the server kernel. -->
    <ProjectReference Include="..\GameServer.Protocol\GameServer.Protocol.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
